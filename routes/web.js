'use strict';

/**
 * routes/web.js
 * ─────────────────────────────────────────────────────────────
 * Entry point untuk semua Web (HTML) routes.
 * Re-export dari src/core/web-router.js — seperti routes/web.php di Laravel.
 *
 * Untuk menambah halaman baru, edit src/core/web-router.js
 * atau tambahkan route langsung di sini.
 */
module.exports = require('../src/core/web-router');
