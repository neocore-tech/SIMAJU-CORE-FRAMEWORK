'use strict';

/**
 * Plugin Initialization
 * This function is called during the framework boot process.
 */
module.exports = async (app) => {
  // You can register middlewares, events, or other logic here.
  // console.log('Hello World Plugin initialized!');
};
